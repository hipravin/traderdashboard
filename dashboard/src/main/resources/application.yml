server:
  port: 6007

---
spring:
  config:
    activate:
      on-profile: local

app:
  loader:
    storage-dir: C:/dev/moex-data/data
logging:
  level:
    org.springframework: INFO

---
spring:
  config:
    activate:
      on-profile: docker

app:
  loader:
    storage-dir: /usr/local/moex-data
logging:
  level:
    org.springframework: INFO